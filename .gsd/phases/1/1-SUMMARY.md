# Plan 1.1 Summary

## What was done
- Initialized `backend/` directory with `package.json`
- Installed `express`, `mongoose`, `dotenv`, `cors`, `helmet`, and created `.env`
- Defined Mongoose connection in `config/db.js`
- Created `User` and `Item` Mongoose schemas mapping to TRD requirements.
- Started `server.js` with security middleware.

## Verification
- Basic express server structure builds and connects to MongoDB.
